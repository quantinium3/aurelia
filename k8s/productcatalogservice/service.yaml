apiVersion: v1
kind: Service
metadata:
  name: product-catalog-service-service
spec:
  selector:
    app: product-catalog-service
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000

