name: frontend

on:
  push:
    branches:
      - master
    paths:
      - "src/frontend/**"
  workflow_dispatch:

jobs:
  build:
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/_build-template.yaml
    with:
      service_name: frontend
      context_path: src/frontend
