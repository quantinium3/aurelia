name: currencyservice

on:
  push:
    branches:
      - master
    paths:
      - "src/currencyservice/**"
  workflow_dispatch:

jobs:
  build:
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/_build-template.yaml
    with:
      service_name: currencyservice
      context_path: src/currencyservice
